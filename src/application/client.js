/**
 * @fileOverview An application manager for the web
 * @module application/client
 * @requires base
 */

var Base = require( "../base" );

/**
 * A client application object. Inherit and have fun with it.
 * @exports application/client
 * @constructor
 */
var Client = Base.compose( [Base], /** @lends application/client# */{

} );
